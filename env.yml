name: alphagenome_ribo312
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pytorch
  - pytorch-cuda=12.1 # Update this if your HPC uses a different CUDA version
  - numpy
  - pandas
  - matplotlib
  - safetensors
  - pip:
      - tqdm
      - pyBigWig
      - pyfaidx
